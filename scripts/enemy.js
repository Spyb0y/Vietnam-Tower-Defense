vtd.enemy = (function () {
    
    function Enemy(url, posX, posY, h, w, speedX, speedY, armor, health) {
        this.posX = posX;
        this.posY = posY;
        this.h = h;
        this.w = w;
        this.size = size;
        this.speedX = 3;
        this.speedY = 3;
        this.url = url;
        this.checkpoints = [];

        var checkpoint1 = { x: 50, y: 25 };
        checkPoints.push(checkpoint1);
        var checkpoint2 = { x: 50, y: 50 };
        checkPoints.push(checkpoint2);

        var index = 0;
        var target = checkPoints[index];

        function update()
        {
          
            
        }
     function killEnemy(enemy)
     {

     }

     function draw(ctx)
     {
         
     }
    
     return {
         draw: draw,
         update : update
     };
    }; 


    

 

   

    





})();